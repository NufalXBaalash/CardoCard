import 'package:flutter/material.dart';

class CardoTheme {
  // CardoCard Theme Colors
  static const Color cardoBlue = Color(0xFF00A2E1); // Updated primary color
  static const Color cardoCardBlue =
      Color(0xFF33B5E5); // Adjusted to match new primary
  static const Color cardoLightBlue =
      Color(0xFF66C7EF); // Lighter shade of primary

  // General Theme Colors
  static const Color accentColor =
      Color(0xFF33B5E5); // Adjusted to match new primary
  static const Color primaryColor = Color(0xFF00A2E1); // Updated primary color
  static const Color secondaryColor =
      Color(0xFF66C7EF); // Lighter shade of primary

  // Backgrounds
  static Color lightBackground = Colors.grey[50]!;
  static Color darkBackground = Color(0xFF212121);

  // Text colors
  static Color lightTextColor = Colors.black87;
  static Color darkTextColor = Colors.white;
  static Color secondaryTextColor = Colors.grey[600]!;
}
